@page "/Event/GroupDate"
@attribute [Authorize]
@inherits PageCore<GroupDate>
@inject HttpClient Http

<FeatureUnavailable></FeatureUnavailable>

@code {
    protected override async Task LoadData()
    {
        //ignore
    }
}