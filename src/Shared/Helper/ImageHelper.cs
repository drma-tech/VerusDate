using System.IO;

namespace VerusDate.Shared.Helper
{
    public static class ImageHelper
    {
        public static bool ValidImage(Stream stream)
        {
            return true;
        }
    }
}