@page "/Event/BlindDate"
@attribute [Authorize]
@inherits PageCore<BlindDate>

<FeatureUnavailable></FeatureUnavailable>

@code {
    protected override async Task LoadData()
    {
       //ignore
    }
}