@page "/Event/SpeedDating"
@attribute [Authorize]
@inherits PageCore<SpeedDating>

<FeatureUnavailable></FeatureUnavailable>

@code {
    protected override async Task LoadData()
    {
        //ignore
    }
}