@page "/login-providers"

<h1>Login / Registro</h1>
<br />

<Row>
    <Column ColumnSize="ColumnSize.Is1.OnMobile.Is1.OnDesktop.Is2.OnFullHD"></Column>
    <Column ColumnSize="ColumnSize.Is12.OnMobile.Is10.OnDesktop.Is8.OnFullHD">
        @foreach (var provider in providers)
        {
            var link = provider.Key;
            if (link == "microsoft") link = "aad";
            <Button Block="true" Class="@("btn-" + @provider.Key)" Size="Size.Large" Margin="Margin.Is3.FromBottom" Type="ButtonType.Link" To="@("/.auth/login/" + @link)">
                <span class="fab fa-@provider.Key"></span> Logar com @provider.Value
            </Button>
        }
    </Column>
</Row>

@code {
    public Dictionary<string, string> providers = new Dictionary<string, string>
    {
        { "google", "Google" },
        { "facebook", "Facebook" },
        { "twitter", "Twitter" },
        { "microsoft", "Microsoft"}
    };
}