@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/login-providers?returnUrl={Uri.EscapeDataString(Navigation.Uri)}");
    }
}