@inherits ComponenteCore<BtnLike>
@inject HttpClient Http

@if (IsVisible)
{
    <Button Color="Color.Primary" Size="Size.Large" Style="position: relative;" title="Like" Clicked="Like" Margin="Margin.Is2.FromLeft">
        <Badge Color="Color.Warning" Style="position: absolute; top: -8px; right: -10px;" title="-1 Maça">
            <Icon Name="Blazorise.Icons.FontAwesome.FontAwesomeIcons.AppleAlt"></Icon>
        </Badge>
        <Icon Name="Blazorise.Icons.FontAwesome.FontAwesomeIcons.ThumbsUp"></Icon>
    </Button>
}

@code {
    [Parameter] public string IdUserView { get; set; }
    [Parameter] public EventCallback OnClick { get; set; }

    private bool IsVisible => true;

    private async Task Like()
    {
        try
        {
            var result = await Http.Interation_Like(IdUserView, SessionStorage);
            await result.ProcessResponse(Toast, msgInfo: "-1 Maça");

            var interaction = await Http.Interation_Get(SessionStorage, IdUserView);

            if (interaction.Match.Value == true)
            {
                Toast.ShowSuccess("Match");
            }

            await OnClick.InvokeAsync(null);
            RefreshCore.RefreshHead(); //need uddate apples
        }
        catch (Exception ex)
        {
            ex.ProcessException(Toast, Logger);
        }
    }
}